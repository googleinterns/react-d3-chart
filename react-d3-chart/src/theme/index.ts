import * as d3 from 'd3';

export const DEFAULT_COLOR = d3.scaleOrdinal(d3.schemeCategory10);

export const DEFAULT_GRAPH_MARGIN = {
  top: 30,
  left: 30,
  right: 30,
  bottom: 30,
};

export const DEFAULT_GRAPH_PADDING = {
  left: 30,
  right: 30,
  bottom: 40,
  top: 30,
};
